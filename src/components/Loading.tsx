const Loading = () => {
    return (
        <div className="flex justify-center items-center w-full py-4">
            <p className="text-center text-lg font-semibold text-red-500 animate-pulse">Loading...</p>
        </div>
    );
};

export default Loading;
